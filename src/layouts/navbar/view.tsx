// import { changeLanguage } from "../../helpers/api";
// import { useTranslation } from "react-i18next";

const Navbar = () => {
  // const { t, i18n } = useTranslation();

  return <nav className="bg-white z-[99]"></nav>;
};

export default Navbar;
